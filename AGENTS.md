# Agent konteksti — demo-backend

Qatlam: **BE**. Faqat `[BE]` yorlig'idagi AC'lar.

Qoidalar: `uyqur-standards` plugin → `rules/dev-rules.md`
Spec: `docs/specs/<CU-id>.md` (`status: approved` bo'lishi shart)
Test yorlig'i: `<CU-id> AC-<n>:` test nomining boshida
Kontrakt o'zgarsa: `uyqur-lab/contracts` ga PR

Taqiqlar: `main` ga push, `git merge`, `gh pr merge`, ClickUp'ga yozish.
